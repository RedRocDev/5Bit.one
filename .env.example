# Public base URL of the NeoStation Assets API (Go service).
# Inlined at build time by the /arts (submissions) and /admin (panel) pages.
# Local dev: http://localhost:8090 (the dockerized API service)
# Production: https://assets-api.neostation.dev
PUBLIC_ASSETS_API_URL=http://localhost:8090